import styled from 'styled-components';

const Card = styled.div`
  width: 280px;
  margin-bottom: 24px;
  position: relative;
  .delete {
    position: absolute;
    right: -15px;
    top: -15px;
  }
`;

export default Card